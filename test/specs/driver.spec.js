'use strict';

var Devicehive = source("driver");

describe("Cylon.Drivers.Devicehive", function() {
  var driver = new Devicehive({
    device: { connection: 'connect' }
  });

  it("needs tests");
});
